spring:
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://user-db:3306/users?serverTimezone=UTC&useLegacyDatetimeCode=false&useSSL=false
    username: usercore
    password: usercore
